options(papersize="a4")
options(editor="emacs")
options(pdfviewer="evince")

.First <- function() {
  require(quantmod)
  require(RSQLite)
  require(ggplot2)
  require(stats)
  setwd("/home/irocha/git/R-trader")
  source("trader.r")
}
